<h1>Admin::Orders#index</h1>
<p>Find me in app/views/admin/orders/index.html.erb</p>

<% @orders.each do |order| %>
    <H1> <%= order.id %> </H1>
    <%= link_to 'Details', admin_order_path(order) %>
    <%= link_to 'Delete', admin_order_path(order),method: :delete %>
    <%= link_to 'Products in Order', admin_order_order_products_path(order) %>
    <br>
<% end %>